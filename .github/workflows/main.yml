on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: |
          sudo apt-get -y update
          sudo apt-get -y install podman

      - run: podman build .
